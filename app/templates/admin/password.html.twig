{% extends 'base.html.twig' %}

{% block title %}
    {{ 'password_edit'|trans({'%id%': admin.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'admin_password'|trans({'%id%': admin.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'PUT', action: url('admin_password', {id: admin.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action_save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('admin_show',{id: admin.id}) }}" class="btn btn-primary">
            {{ 'action_back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}


